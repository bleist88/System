# TCSHRC

##  To update changes made in the .tcshrc file...
##      source .tcshrc

##  My shell can be switched to a bash shell via...
##      chsh -s /bin/bash/
##  Unfortunately, I don't have permissions to do so.

## PATHS

setenv  home            /home/bleist

setenv  mac_path        /usr/local/bin:/usr/bin:/bin:/usr/sbin:/opt/X11/bin
setenv  hub_path        /usr/lib64/mpi/gcc/openmpi/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin:/usr/bin/X11:usr/X11R6/bin:/usr/lib/qt3/bin
setenv  usr_path        ${home}/System/Scripts:.
setenv  ext_path        /anaconda3/bin
setenv  PATH            ${hub_path}:${usr_path}:${ext_path}

setenv  PYTHONPATH	    /home/bleist/System/Library/

## Variables

setenv  bleist88        https://github.com/bleist88

## Aliases

alias   ds9		       'ds9_72'
alias   python		   '/astro/anaconda3/bin/ipython3'
alias   gpush          'git add -A; git commit -m "updating"; git push'
alias   gpull          'git pull'
